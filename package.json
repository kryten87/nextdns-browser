{
  "name": "nextdns",
  "version": "0.0.0",
  "main": "index.js",
  "author": "Dave Thomas <davet@ifit.com>",
  "license": "MIT",
  "scripts": {
    "dev:up": "docker compose --env-file ./nestjs-server/.env -f docker-compose.dev.yml up",
    "dev:down": "docker compose --env-file ./nestjs-server/.env -f docker-compose.dev.yml down"
  }
}
